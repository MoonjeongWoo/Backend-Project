<table>
            <tr>
                <td>ID&nbsp;</td>
                <td><input type="text" value="id-test" id="id"></td>
            </tr>
            <tr>
                <td>PW&nbsp;</td>
                <td><input type="text" value="mypw" id="pw"></td>
                <td><button type="button" onclick="login()">LOGIN</button></td>
            </tr>

        </table>
    </form>
    <script>
        function login() {
            var id = $("#id").val();
            var pw = $("#pw").val();
            var data = {
                id: id,
                pw: pw
            }
            axios({
                url: "/userLogin",
                method: "POST",
                data: data
            }).then((response) => {
                console.log(response.data.result); 
                 if(response.data.result == 1) {
            $(".login-back").attr("style","display: none");
            $(".login").attr("style","display: none");
            } else {
                alert("올바르지 않은 아이디 혹은 비밀번호 입니다.");
            }
            });
        }
      
    </script>